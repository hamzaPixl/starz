# Starz — Sprint Plan

> Generated by /sprint-planning from `.context/task-state.json`
> Generated at: 2026-04-03
> Total: 21 tasks, 35 points across 3 sprints

---

## Sprint 1: CLI-First — sync, search, and chat work in terminal

**Goal**: A user can `pip install starz`, run `starz sync` to pull all their GitHub stars with AI categorization, then `starz search` and `starz chat` to find repos from the terminal.

Branch: `feat/sprint-1-cli-core`
Capacity: 20 points | Planned: 17 points (buffer: 3 points)
Velocity basis: default estimate (20 pts, greenfield -20%)

| # | Task ID | Task | Size | Pts | Agent | Depends On | Status |
|---|---------|------|------|-----|-------|------------|--------|
| 1 | T-01 | Project scaffold: pyproject.toml, src layout, CLI entry point | M | 2 | backend-engineer | — | pending |
| 2 | T-02 | Config module: env loading, XDG data paths | S | 1 | backend-engineer | T-01 | pending |
| 3 | T-03 | Database layer: SQLite + sqlite-vec client and schema | M | 2 | backend-engineer | T-02 | pending |
| 4 | T-04 | GitHub service: fetch stars + READMEs | L | 3 | backend-engineer | T-03 | pending |
| 5 | T-05 | Pydantic schemas: repo and chat models | S | 1 | backend-engineer | T-01 | pending |
| 6 | T-06 | Embedding service: OpenAI vector generation + sqlite-vec storage | M | 2 | backend-engineer | T-03 | pending |
| 7 | T-07 | Categorizer service: Claude Haiku batch classification | M | 2 | backend-engineer | T-03 | pending |
| 8 | T-08 | Sync CLI command: wire fetch + embed + categorize pipeline | S | 1 | backend-engineer | T-04, T-06, T-07 | pending |
| 9 | T-09 | Search service + CLI command: vector + keyword search | M | 2 | backend-engineer | T-06, T-08 | pending |
| 10 | T-10 | Chat service + CLI command: RAG pipeline | M | 2 | backend-engineer | T-09 | pending |

### Execution Order (respects dependencies, maximizes parallelism)

```
T-01 ──→ T-02 ──→ T-03 ──┬──→ T-04 ──┐
  │                       ├──→ T-06 ──┤
  │                       └──→ T-07 ──┤
  └──→ T-05                           ├──→ T-08 ──→ T-09 ──→ T-10
                                      │
                          (parallel)  ┘
```

**Parallel opportunities:**
- T-04, T-06, T-07 can run in parallel after T-03
- T-05 can run in parallel with T-02/T-03

### Acceptance Criteria
- [ ] `pip install -e .` succeeds and `starz --help` shows commands (R-150)
- [ ] `starz sync` fetches all starred repos with pagination (R-001)
- [ ] READMEs fetched in parallel with 10 concurrency limit, 5s timeout (R-002, R-101)
- [ ] All repos stored in SQLite with sqlite-vec extension loaded (R-003, R-151)
- [ ] Embeddings generated via OpenAI text-embedding-3-small, stored in vec0 table (R-004)
- [ ] Repos auto-categorized by Claude Haiku in batches of 20 (R-005)
- [ ] `starz search "react animation"` returns semantically relevant repos (R-008)
- [ ] `starz chat` opens interactive REPL with accurate RAG responses (R-009)
- [ ] Sync pipeline handles 500+ repos without crashing (R-101)
- [ ] API failures (GitHub/OpenAI/Anthropic) are caught and don't crash the pipeline (R-102)

### Risks
- **sqlite-vec on macOS ARM**: First real integration — test early in T-03. Mitigation: pip wheel confirmed for arm64, fallback to source build.
- **GitHub token detection**: `gh auth token` subprocess may fail in some envs. Mitigation: GITHUB_TOKEN env var as primary, gh as fallback.
- **Embedding cost accuracy**: Need to validate actual token usage matches $0.01 estimate. Mitigation: log token counts during T-06.

### Git Strategy
- All S/M tasks: single commit on `feat/sprint-1-cli-core`
- T-04 (L): separate branch `feat/T-04-github-service` → merge into sprint branch
- PR to `main` after Sprint 1 acceptance criteria pass

---

## Sprint 2: Web UI — full browser experience with API server

**Goal**: `starz serve` launches a web UI where users browse repos in a grid, filter by category/language, search semantically, and chat with their stars — all on a single port.

Branch: `feat/sprint-2-web-ui`
Capacity: 20 points | Planned: 15 points (buffer: 5 points)
Velocity basis: default estimate (20 pts), extra buffer for frontend greenfield

| # | Task ID | Task | Size | Pts | Agent | Depends On | Status |
|---|---------|------|------|-----|-------|------------|--------|
| 1 | T-11 | FastAPI server: app factory + repo/stats endpoints | M | 2 | backend-engineer | T-03, T-05 | pending |
| 2 | T-12 | API endpoints: sync + sync status | S | 1 | backend-engineer | T-05, T-08 | pending |
| 3 | T-13 | API endpoints: search + chat | S | 1 | backend-engineer | T-09, T-10, T-11 | pending |
| 4 | T-14 | Web scaffold: Next.js + Tailwind + shadcn/ui with static export | M | 2 | frontend-engineer | T-01 | pending |
| 5 | T-15 | Web: repo browse page — grid, cards, filters, search bar | M | 2 | frontend-engineer | T-14 | pending |
| 6 | T-16 | Web: chat panel component | M | 2 | frontend-engineer | T-13, T-14 | pending |
| 7 | T-17 | Web: sync button + static bundle into Python package | S | 1 | fullstack-engineer | T-15, T-16 | pending |

### Execution Order

```
T-11 ──┐
T-12 ──┤──→ T-13 ──┐
       │            │
T-14 ──┼──→ T-15 ──┤──→ T-17
       └──→ T-16 ──┘
```

**Parallel opportunities:**
- T-11, T-12, T-14 can all start immediately (Sprint 1 deps satisfied)
- T-15 and T-16 can run in parallel after T-14

### Acceptance Criteria
- [ ] `starz serve` starts FastAPI on port 7827 (R-007)
- [ ] GET /api/repos returns paginated list with ?category=, ?language=, ?q= filters (R-007)
- [ ] GET /api/repos/{id} returns single repo detail or 404 (R-010)
- [ ] GET /api/stats returns counts grouped by category and language (R-011)
- [ ] POST /api/sync triggers background sync, GET /api/sync/status reports progress (R-006, R-012)
- [ ] POST /api/search returns ranked semantic results (R-008)
- [ ] POST /api/chat returns RAG response with sources (R-009)
- [ ] Web UI shows responsive repo grid with cards (R-013)
- [ ] Category/language sidebar filters work (R-013)
- [ ] Chat panel shows conversation with clickable repo links (R-014)
- [ ] Sync button triggers sync with progress indicator (R-015)
- [ ] `make build-web` produces static bundle, served by FastAPI (R-152)
- [ ] All API endpoints respond in < 500ms (except chat < 5s) (R-100)

### Risks
- **Static export compatibility**: Next.js static export doesn't support all features (no API routes, no SSR). Mitigation: only use client components with fetch.
- **CORS during development**: Dev server on :3000 hitting API on :7827. Mitigation: CORS middleware configured in T-11.
- **Chat panel UX**: First time building chat UI — may need iteration. Extra buffer allocated.

### Git Strategy
- T-11, T-12, T-13: commits on `feat/sprint-2-web-ui`
- T-14, T-15, T-16: can use `feat/T-14-web-scaffold` branch if preferred
- T-17: final integration commit
- PR to `main` after all acceptance criteria pass

---

## Sprint 3: OSS Polish — packaging, docs, incremental sync

**Goal**: Project is publishable to PyPI. Anyone can `pip install starz` and be running in 5 minutes. Incremental sync makes re-runs fast.

Branch: `feat/sprint-3-oss-polish`
Capacity: 20 points | Planned: 5 points (buffer: 15 points — intentionally light for iteration)
Velocity basis: default estimate

| # | Task ID | Task | Size | Pts | Agent | Depends On | Status |
|---|---------|------|------|-----|-------|------------|--------|
| 1 | T-18 | OSS polish: README, .env.example, error handling | S | 1 | fullstack-engineer | T-17 | pending |
| 2 | T-19 | PyPI packaging: metadata, classifiers, entry points | S | 1 | backend-engineer | T-18 | pending |
| 3 | T-20 | Incremental sync: diff-based re-sync | S | 1 | backend-engineer | T-08 | pending |
| 4 | T-21 | CLAUDE.md: project conventions and operational rules | S | 1 | doc-updater | T-18 | pending |

### Execution Order

```
T-18 ──┬──→ T-19
       └──→ T-21
T-20 (independent — can start anytime after Sprint 1)
```

**Note**: T-20 (incremental sync) has no Sprint 2 dependencies — can be pulled into Sprint 2 if velocity allows.

### Acceptance Criteria
- [ ] README.md has install, quickstart, CLI reference, API reference (—)
- [ ] `pip install .` in clean venv produces working `starz` CLI (—)
- [ ] Static files included in installed package (—)
- [ ] Second `starz sync` only processes new repos (R-016)
- [ ] Summary shows "N new, N updated, N skipped" (R-016)
- [ ] CLAUDE.md accurately documents project for contributors (—)
- [ ] .env.example documents all 3 required env vars (—)

### Risks
- **PyPI naming conflict**: "starz" may be taken on PyPI. Mitigation: check early, fallback to "gh-starz" or "starz-cli".
- Low risk sprint overall — mostly documentation and packaging.

### Git Strategy
- All tasks: commits on `feat/sprint-3-oss-polish`
- Final PR to `main`, then tag `v0.1.0`

---

## Summary

| Sprint | Goal | Points | Tasks | Key Delivery |
|--------|------|--------|-------|-------------|
| 1 | CLI-First | 17/20 | 10 | `starz sync`, `starz search`, `starz chat` |
| 2 | Web UI | 15/20 | 7 | `starz serve` with full browser UI |
| 3 | OSS Polish | 5/20 | 4 | PyPI package, README, incremental sync |
| **Total** | | **37/60** | **21** | **Open-source `pip install starz`** |

### Velocity Notes
- No historical data — using conservative default (20 pts/sprint)
- Sprint 3 intentionally light to absorb Sprint 1-2 overflow and allow iteration
- Total buffer across 3 sprints: 23 points (39%) — appropriate for greenfield
- After Sprint 1 completes, recalibrate velocity with actual data

### Downstream Integration
- After each sprint: run `/spec-review .context/prd.md` to validate coverage
- After Sprint 3: run `/changelog` to generate release notes for v0.1.0
- Continuous: `/self-review-fix-loop` before each PR merge
